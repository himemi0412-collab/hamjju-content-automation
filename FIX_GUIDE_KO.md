# 햄쮸 자동화 수정본 적용 안내

> 과거 적용 기록입니다. 현재 동작과 검증 순서는 README.md 및 docs/automation-reliability.md를 기준으로 확인하세요.

## 확인된 원인

1. `app/main.py`의 예외는 원인이 아니라 QA 실패를 알리는 안전장치였습니다.
2. 쇼츠는 1차 대본 QA가 실패하면 미디어 생성 전에 정상적으로 중단됐지만, 자동 재시도가 없어 매일 같은 방식으로 종료됐습니다.
3. QA의 구체적인 `blocking_issues`가 최종 결과에 포함되지 않아 Actions 로그만으로 수정 사유를 확인하기 어려웠습니다.
4. 블로그 카드는 렌더링 QA가 실패했는데도 Notion에 먼저 첨부되고 있었습니다.
5. 삐죽이 주제 조사에 일본 쇼츠 제목까지 하나의 목록으로 전달되어 일본 소재가 삐죽이 채널로 섞일 수 있었습니다.
6. 수동 `dry_run`은 읽기 전용이라 빈 대기열에 주제를 준비할 방법이 없었습니다.

## 수정된 파일

- `.github/workflows/daily.yml`
- `app/main.py`
- `app/pipeline.py`
- `prompts/topic_radar.md`
- `tests/test_blog_output_verification.py`
- `tests/test_main_results.py`

## 수정된 동작

- 예약 실행은 QA 반려 항목만 한 번 자동 재생성·재검수합니다.
- 네트워크·인증·코드 오류는 자동 재시도로 숨기지 않고 그대로 실패 처리합니다.
- QA 실패 카드와 원고는 Notion에 첨부하거나 본문 블록으로 저장하지 않습니다.
- Actions 결과에 `blocking_issues`와 `qa_score`가 표시됩니다.
- 삐죽이와 일본 쇼츠의 기존 제목 목록을 채널별로 분리합니다.
- 네이버 공개·예약 발행은 계속 금지되며, 운영 계약의 `publication_owner: user`를 변경하지 않습니다.
- YouTube 자동 업로드 기능은 제거했습니다. 인증 확인 명령은 채널 신원 조회에만 사용합니다.
- 수동 실행에 `prepare_topic`을 추가했습니다. 선택한 채널의 주제만 1개 조사해 Notion에 저장하며 원고·이미지·영상·업로드는 실행하지 않습니다.
- 삐죽이는 확인된 실제 경험 근거가 없으면 `아이디어`로만 저장되고 자동 제작 대상이 되지 않습니다.
- `plan_month`는 30일 기준 블로그 90개·삐죽이 30개·일본 쇼츠 30개의 월간 주제 창고를 10개 이하 묶음으로 준비합니다.
- 월간 계획은 같은 달의 기존 콘텐츠 ID와 블로그 진행 순서를 세어 재실행 시 부족분만 채웁니다.
- 매일 제작 작업은 더 이상 당일 주제를 즉석 생성하지 않고, 미리 준비된 대기열만 소비합니다.
- 블로그 본문은 굵게·밑줄·라벤더 강조·문단 간격·절제된 이모지 규칙을 포함합니다.
- 일본 쇼츠는 이야기별 narrator_profile을 기록하며, 사용자 승인 음성이 없는 프로필은 TTS 전에 중단됩니다.
- 쇼츠 완료 확인은 실제 MP4 파일과 QA 통과로 합니다. YouTube 업로드·공개는 수행하지 않습니다.

## GitHub 웹에서 적용하는 가장 쉬운 방법

이 ZIP은 저장소 전체 수정본입니다. GitHub 웹은 ZIP을 저장소에 한 번에 덮어쓰는 기능이 없으므로 다음 두 방법 중 하나를 사용합니다.

### 방법 1: GitHub Desktop

1. 기존 저장소를 GitHub Desktop으로 내려받습니다.
2. 이 ZIP을 풀어 나온 파일 전체를 기존 저장소 폴더에 덮어씁니다.
3. GitHub Desktop에서 변경 파일 6개를 확인합니다.
4. 커밋 메시지를 `Fix QA retry and channel isolation`으로 입력합니다.
5. `Commit to main` → `Push origin`을 누릅니다.

### 방법 2: GitHub 웹 Edit

위의 ‘수정된 파일’ 6개를 GitHub에서 하나씩 열고, 수정본의 같은 파일 내용을 전체 복사하여 붙여넣은 뒤 커밋합니다.

## 적용 후 첫 검증

1. `Actions` → `Hamjju Daily Content Production`을 엽니다.
2. `Run workflow`를 누릅니다.
3. 월간 창고를 처음 채울 때는 `mode: plan_month`를 실행합니다. 미디어와 업로드는 실행되지 않습니다.
4. 한 채널만 1건 준비할 때는 원하는 `channel`과 `mode: prepare_topic`을 사용합니다.
5. 같은 채널에서 `mode: dry_run`을 실행해 준비된 항목을 읽는지 확인합니다.
6. 성공하면 쇼츠는 `mode: execute_media`로 1건만 실행합니다.
7. `blocking_issues`가 비어 있고, 실제 MP4 파일과 영상 QA가 확인됐는지 검토합니다.
8. 같은 방식으로 나머지 쇼츠 채널 1건을 확인합니다.
9. 블로그는 공개·예약 발행하지 말고 임시저장 인계 상태만 확인합니다.

## 검증 결과

- Python 문법 검사 통과
- Python 문법 검사 통과
- 전체 회귀 테스트 154개 통과
